import '../fhir_path.dart';

class MillisecondParser extends FhirPathParser {
  MillisecondParser();
  List execute(List results, Map passed) => results;
}

class SecondParser extends FhirPathParser {
  SecondParser();
  List execute(List results, Map passed) => results;
}

class SecondsParser extends FhirPathParser {
  SecondsParser();
  List execute(List results, Map passed) => results;
}

class MinutesParser extends FhirPathParser {
  MinutesParser();
  List execute(List results, Map passed) => results;
}

class HourParser extends FhirPathParser {
  HourParser();
  List execute(List results, Map passed) => results;
}

class HoursParser extends FhirPathParser {
  HoursParser();
  List execute(List results, Map passed) => results;
}

class DayParser extends FhirPathParser {
  DayParser();
  List execute(List results, Map passed) => results;
}

class DaysParser extends FhirPathParser {
  DaysParser();
  List execute(List results, Map passed) => results;
}

class WeeksParser extends FhirPathParser {
  WeeksParser();
  List execute(List results, Map passed) => results;
}

class MonthParser extends FhirPathParser {
  MonthParser();
  List execute(List results, Map passed) => results;
}

class MonthsParser extends FhirPathParser {
  MonthsParser();
  List execute(List results, Map passed) => results;
}

class YearParser extends FhirPathParser {
  YearParser();
  List execute(List results, Map passed) => results;
}

class YearsParser extends FhirPathParser {
  YearsParser();
  List execute(List results, Map passed) => results;
}
