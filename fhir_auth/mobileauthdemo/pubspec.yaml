name: mobileauthdemo

environment:
  sdk: '>=2.12.1 <3.0.0'

dependencies:
  cupertino_icons: ^1.0.3
  fhir: ^0.4.6
  fhir_auth:
    path: ../../fhir_auth
  flutter:
    sdk: flutter
  openid_client: ^0.4.1
  url_launcher: ^6.0.10
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true