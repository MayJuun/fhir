name: mobileauthdemo

environment:
  sdk: '>=2.14.0 <3.0.0'

dependencies:
  cupertino_icons: ^1.0.3
  fhir: ^0.5.0-2
  fhir_auth:
    path: ../../fhir_auth
  flutter:
    sdk: flutter
  google_sign_in: ^5.1.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true