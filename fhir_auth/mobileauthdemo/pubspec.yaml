name: mobileauthdemo

environment:
  sdk: '>=2.14.0 <3.0.0'

dependencies:
  cupertino_icons: ^1.0.3
  fhir: ^0.5.0-2
  fhir_auth:
    path: ../../fhir_auth
  flutter:
    sdk: flutter
  openid_client: ^0.4.1
  url_launcher: ^6.0.10
  google_sign_in: ^5.1.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true