name: authdemo
environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.2
  fhir: 
    path: ../../fhir
  fhir_at_rest:
    path: ../../fhir_at_rest
  fhir_auth:
    path: ../../fhir_auth

dev_dependencies:
  flutter_test:
    sdk: flutter